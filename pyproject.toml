[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"


[project]
name = "cockpit-test-framework"
version = "v0.1.0"
description = "AI-powered cockpit domain testing framework"
requires-python = ">=3.10"


[tools.pytest.ini_options]
testpaths = ["tests"]